# Ralph Foulger's School of Real Estate — Continuation Note

> Saved 2026-05-10. Pick up here on session resume.

## Current state

- **Live:** https://pono-realty-production.up.railway.app
- **Last commit:** `dee9679` (Ralph credentials + AI audit promise + graduation reward)
- **Local ↔ remote ↔ Railway:** all synced

## Three pending human actions (only Zach can do)

### 1. Set `ANTHROPIC_API_KEY` on Railway
- Go to: https://railway.com/project/3dea793e-1c94-453e-bc70-6711fcab748d
- Service `pono-realty` → Variables → + New Variable
- Name: `ANTHROPIC_API_KEY`
- Value: from https://console.anthropic.com/settings/keys (separate from claude.ai sub)
- $5 free credit covers ~250 conversations for testing
- **The instant this is set, the AI tutor at `/tutor` goes from "being provisioned" to fully working.**

### 2. Wire custom domain ralphfoulger.com
- Railway → service → Settings → Networking → + Custom Domain
- Enter `ralphfoulger.com`
- Railway returns a CNAME target
- At registrar (where ralphfoulger.com was bought):
  - Advanced DNS → CNAME `@` (or ALIAS/A record if `@` CNAME not allowed) → Railway target
  - CNAME `www` → same target
- Wait 5-30 min for DNS propagation
- Railway auto-issues Let's Encrypt SSL

### 3. Reconnect Railway → GitHub auto-deploy
- Railway → Settings → Source
- Disconnect: `celestialarchitect-ux/pono-realty` (old name, broken)
- Connect: `celestialarchitect-ux/ralph-foulger-school` (current)
- Branch: `main`
- After: every push auto-deploys (currently using `railway up --detach` manually)

## After all three above are done, ask Claude to:

1. Update `SITE_URL` env var on Railway from current to `https://ralphfoulger.com`
2. Update OG / canonical / Twitter metadata in `src/app/layout.tsx` to point at the new domain
3. Verify HTTPS cert + decide www → apex or apex → www redirect
4. Test AI tutor end-to-end: send a question, verify Hawaii-tagged answer streams in
5. Update Cartographer's project map to reflect the live domain

## What's already shipped

- Full rebrand from Pono Realty → Ralph Foulger's School of Real Estate
- 20-chapter PSI-aligned curriculum (pre-existing, intact)
- 5 free lesson pages with substantive content + Web Speech API audio player
- AI tutor chat UI + streaming Claude API route (waiting on key)
- 3-tier pricing ($0 / $599 / $899), no subscription, one-time payment
- Graduation Reward Package marketing section (first-try pass unlocks website + leads + onboarding)
- AI accuracy promise section (human-audited weekly)
- Why Ralph section with real 54-year credentials
- PWA manifest, icon.svg, apple-icon, viewport-fit=cover
- Mobile-responsive (hamburger drawer, safe-area-inset, 44px touch, 16px input no-zoom)
- Security headers (HSTS, CSP, X-Frame-Options, Permissions-Policy, no X-Powered-By)
- Motivation modal (first-visit, rewritten v2 — "Your future is in your hands")
- 20 conversion posts at `~/claude-dashboard/_ralph-marketing/20-conversion-posts.md`

## Commands that matter

```bash
# manually deploy when GitHub auto-deploy is broken
cd ~/claude-dashboard/ralph-foulger-school && railway up --detach

# check Railway service state
railway status

# inspect env vars
railway variables --service pono-realty

# typecheck + build before commit
npm run build

# Hawaii market data files (verified)
# - 922 candidates tested 2024-2025
# - 40-45% statewide pass rate
# - source: cca.hawaii.gov/reb/school-files-2021-2025/
```

## Resume prompt to give Claude

> "Continue the Ralph Foulger's School project. Read `~/claude-dashboard/ralph-foulger-school/.continue-here.md` and the project memory file. The site is live at pono-realty-production.up.railway.app. I want to [X]." 

(replace X with the actual next request — domain wiring, API key setup verification, building new feature, etc.)
